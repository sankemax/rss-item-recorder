{
  "name": "rss-item-recorder",
  "version": "1.0.0",
  "description": "record rss items from davereader to sqlite-db using event emitter",
  "main": "src/index.js",
  "scripts": {
    "eslint": "npx eslint src/**/*.js",
    "start": "npm run start:dev",
    "start:dev": "npm run eslint && cross-env NODE_ENV=dev node --inspect src/index.js",
    "start:prod": "npm run eslint && cross-env NODE_ENV=prod node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sankemax",
  "license": "ISC",
  "dependencies": {
    "config": "^3.2.5",
    "cors": "^2.8.5",
    "davereader": "^0.6.10",
    "express": "^4.17.1",
    "moment": "^2.24.0",
    "parse-url": "^5.0.1",
    "promise-queue": "^2.2.5",
    "ramda": "^0.26.1",
    "sqlite3": "^4.1.1"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "eslint": "^6.8.0"
  }
}
